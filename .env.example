# .env.example — copy to .env and fill in values (never commit .env)
# No secrets are required for the core MVP.
# Future integrations (Gmail API, Upwork API if approved) would go here.

# Example only — leave blank if not using
# GMAIL_CLIENT_ID=
# GMAIL_CLIENT_SECRET=
