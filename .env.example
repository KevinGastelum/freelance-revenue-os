# Freelance Revenue OS — environment variables example
# Copy to .env and fill in values. Never commit .env to git.

# Optional: override database path
# FREELANCE_OS_DB_PATH=./data/freelance_os.sqlite

# Optional: override config path
# FREELANCE_OS_CONFIG=./config/settings.toml
